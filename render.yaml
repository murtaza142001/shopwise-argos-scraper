services:
  - type: web
    name: shopwise-argos-scraper
    runtime: python
    buildCommand: ./render-build.sh
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHONUNBUFFERED
        value: '1'
    plan: free
